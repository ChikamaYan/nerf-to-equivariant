# To run: conda env create -f environment.yml
name: nerf-profiler
channels:
    - conda-forge
dependencies:
    - python=3.7
    - pip
    - cudatoolkit
    - numpy
    - matplotlib
    - imageio
    - imageio-ffmpeg
    - configargparse
    - imagemagick
